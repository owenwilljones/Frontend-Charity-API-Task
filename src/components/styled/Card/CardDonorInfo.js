/**
 * Styled component for donor card basic donation info
 */

import styled from 'styled-components';

export default styled.div`
    grid-column-start: 2;
    grid-column-end: 4;
    text-align: right;
`;