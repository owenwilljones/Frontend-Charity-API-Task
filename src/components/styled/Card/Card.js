/**
 * Styled component for donor card
 */

import styled from 'styled-components';

export default styled.div`
    background: #d9d9d9;
    padding: 1em;
    display: grid;
    grid-template-columns: 1fr auto auto;
    grid-row-gap: 1em;
    grid-column-gap: 1em;
`;